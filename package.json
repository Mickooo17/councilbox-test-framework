{
  "name": "playwright-test-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test-ui": "npx playwright test --ui",
    "smoke": "npx playwright test --grep \"@smoke\"",
    "regression": "npx playwright test --grep \"@regression\"",
    "report": "allure generate allure-results --clean -o allure-report && npx allure open allure-report",
    "clean": "rmdir /s /q allure-results && rmdir /s /q allure-report && rmdir /s /q playwright-report && rmdir /s /q test-results"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.1",
    "@typescript-eslint/eslint-plugin": "^8.43.0",
    "@typescript-eslint/parser": "^8.43.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.3.3",
    "eslint": "^9.35.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
